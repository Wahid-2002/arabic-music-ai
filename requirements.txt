# Bulletproof requirements for Python 3.13 compatibility
# Only using packages that are guaranteed to work

Flask==2.3.3
Flask-SQLAlchemy==3.0.5
Flask-CORS==4.0.0
Werkzeug==2.3.7
gunicorn==20.1.0

# No PostgreSQL adapter - use SQLite only for now
# We'll add database support after successful deployment

# Basic utilities only
requests==2.31.0
